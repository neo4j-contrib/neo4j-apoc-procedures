package apoc;

public enum ExtendedSystemPropertyKeys
{
    // cypher stored procedures/functions
    inputs,
    description,
    mode,
    outputs,
    output,
    forceSingle,
    prefix,

    // triggers
    selector,
    params,
    paused,

    // dv
    data,

    // uuid handler
    label,
    addToSetLabel,
    addToExistingNodes,
    propertyName
}
